{#
/**
 * @file
 * Theme override for an 'input' #type form element.
 *
 * Available variables:
 * - attributes: A list of HTML attributes for the input element.
 * - children: Optional additional rendered elements.
 *
 * @see template_preprocess_input()
 */
#}
{# <input{{ attributes.setAttribute("placeholder","Buscador...") }} />{{ children }} #}
{# <button {{ attributes.removeAttribute("value") }} />{{ children }}<img src="{{ url('<front>') }}/sites/default/files/2021-12/lupa.png"> </button>  #}
<input{{ attributes.setAttribute('placeholder', 'Buscador...') }} />{{ children }}